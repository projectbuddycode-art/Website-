{
  "name": "project-buddy-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Project Buddy production marketing site and project inquiry endpoint.",
  "scripts": {
    "generate:pages": "node scripts/generate-pages.mjs",
    "lint": "node scripts/lint-site.mjs",
    "typecheck": "node --check scripts/generate-pages.mjs && node --check scripts/lint-site.mjs && node --check scripts/build-check.mjs",
    "build": "npm run generate:pages && npm run lint && npm run typecheck && node scripts/build-check.mjs",
    "deploy": "npm run build && vercel --prod"
  },
  "dependencies": {},
  "engines": {
    "node": ">=20"
  }
}
